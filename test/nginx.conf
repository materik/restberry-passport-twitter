events {
    worker_connections 768;
}

http {
    server {
        listen 80;
        root /app;
        include /etc/nginx/mime.types;

        location / {
            index index.html;
        }
    }
}
